<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marciano.aps1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".geral.LoginActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".geral.CadastroActivity"
            android:label="@string/cadastro_title" />
        <activity
            android:name=".responsavel.CadastroMetasActivity"
            android:label="@string/cadastro_metas_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".responsavel.MenuOpcoesActivity" />
        <activity android:name=".filho.MinhasMetasActivity" />
        <activity android:name=".filho.PerguntasActivity" />
        <activity android:name=".responsavel.VincularFilhosActivity" />
        <activity android:name=".filho.PrincipalActivity"></activity>
    </application>

</manifest>